name: layout_motion
description: >-
  Automatic FLIP layout animations for Flutter. Wrap any Column, Row, or Wrap
  to animate add, remove, and reorder with zero configuration.
version: 0.2.0
repository: https://github.com/meylis1998/layout_motion
issue_tracker: https://github.com/meylis1998/layout_motion/issues
topics:
  - animation
  - layout
  - widget
  - flip
  - transition

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
